# Meeting Notes — {{ meeting.title }}

| Field | Value |
|---|---|
| **Client** | {{ meeting.client or "—" }} |
| **Project** | {{ meeting.project or "—" }} |
| **Date** | {{ meeting.date or "—" }} |
| **Duration** | {{ meeting.duration or "—" }} |
| **Organizer** | {{ meeting.organizer or "—" }} |
| **Provider** | {{ provider_used }} |
| **Generated** | {{ generated_at }} |

---

## Attendees

{% if meeting.attendees %}
{% for a in meeting.attendees %}
- {{ a }}
{% endfor %}
{% else %}
_Attendee list unavailable_
{% endif %}

---

## Summary

{{ summary.one_liner or "_No summary produced_" }}

---

## Topic Breakdown

{% if summary.topics %}
{% for t in summary.topics %}
### {{ t.topic }}
{% for b in t.bullets %}
- {{ b }}
{% endfor %}

{% endfor %}
{% else %}
_No topic breakdown_
{% endif %}

---

## Decisions Made

{% if decisions %}
| # | Decision | Decided By |
|---|---|---|
{% for d in decisions %}
| {{ loop.index }} | {{ d.decision }} | {{ d.decided_by or "—" }} |
{% endfor %}
{% else %}
_No decisions recorded_
{% endif %}

---

## Action Items

{% if action_items %}
| # | Owner | Action | Due |
|---|---|---|---|
{% for a in action_items %}
| {{ loop.index }} | {{ a.owner or "UNCLEAR" }} | {{ a.action }} | {{ a.due or "—" }} |
{% endfor %}
{% else %}
_No action items_
{% endif %}

---

## Open Questions

{% if open_questions %}
{% for q in open_questions %}
{{ loop.index }}. {{ q }}
{% endfor %}
{% else %}
_None_
{% endif %}

---

## Context Extracted

| Category | Items |
|---|---|
| People mentioned | {{ (context.people_mentioned or []) | join(", ") or "—" }} |
| Organizations mentioned | {{ (context.organizations_mentioned or []) | join(", ") or "—" }} |
| Projects mentioned | {{ (context.projects_mentioned or []) | join(", ") or "—" }} |
| Numeric values | {{ (context.numeric_values_mentioned or []) | join(", ") or "—" }} |

---

_Generated by hybrid-meeting-notes. Review before distribution._
